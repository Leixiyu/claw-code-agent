# Claw Code Agent runtime and local GUI dependencies.
# Requires Python 3.10 or newer.

fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
pydantic>=2.5,<3.0

# Used by the video-analysis business Function and FastAPI/Starlette TestClient.
httpx>=0.27,<1.0

# The core model client and .env loader use Python's standard library.
# Do not add vLLM here: install it separately only on a self-hosted model server.
